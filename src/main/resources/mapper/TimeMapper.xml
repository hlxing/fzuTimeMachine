<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.west2.fzuTimeMachine.dao.TimeDao">

	<insert id="save" parameterType="Time" keyProperty="id" useGeneratedKeys="true">
		INSERT INTO
			time(userId, title, imgUrl, content, longitude, latitude, createTime, updateTime, label, praiseNum, checkStatus)
		VALUES
			(#{userId}, #{title}, #{imgUrl}, #{content}, #{longitude}, #{latitude}, #{createTime}, #{updateTime}
				, #{label}, #{praiseNum}, #{checkStatus})
	</insert>

	<update id="updateStatus">
		UPDATE
			time
		SET
			checkStatus = #{status}
		WHERE
			id = #{id}
	</update>
	<select id="getById" resultMap="Time">
		SELECT
			   userId, title, imgUrl, content, longitude, latitude, createTime, updateTime, label, praiseNum, checkStatus
		FROM time
		WHERE id = #{id}
	</select>
	<update id="updateById" parameterType="Time">
		UPDATE
			time
		SET
			<if test="userId != null">
				userId = #{userId},
			</if>
			<if test="title != null">
				title = #{title},
			</if>
			<if test="imgUrl != null">
				imgUrl = #{imgUrl},
			</if>
			<if test="content != null">
				content = #{congten},
			</if>
			<if test="lomgitude != null">
				longitude = #{longitude},
			</if>
			<if test="latitude != null">
				latitude = #{latitude},
			</if>
			<if test="label != null">
				label = #{label},
			</if>
			<if test="praiseNum != null">
				praiseNum = #{praiseNum},
			</if>
			<if test="checkStatus != null">
				checkStatus = #{checkStatus}
			</if>
		WHERE
			id = #{id}
	</update>
</mapper>  